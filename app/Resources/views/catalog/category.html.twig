{% extends 'layout.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    
    <link rel="stylesheet" href="{{ asset('assets/css/items.css') }}" />
{% endblock %}

{% block body %}
    <div class="perso">
        <div id="mySideNav" class="side-nav">
            <a href="#">About</a>
            <a href="#">Services</a>
            <a href="#">Clients</a>
            <a href="#">Contact</a>

            <div class="dropdown-divider"></div>
            <div class="dropdown-divider"></div>

            {% for categorie in categories %}
                <a href="{{ path('category', {'id': categorie.id}) }}">{{ categorie.name }}</a>
            {% endfor %}

            <div class="dropdown-divider"></div>
            <div class="dropdown-divider"></div>
        </div>

        <div class="container">
            {% for product in products if product.idCategorie == id %}
                <div class="row">
                    <div class="product-box">
                        <div class="img-product col-lg-2">
                            <img class="img-product" src="{{asset('assets/img/'~product.getProductBrand)}}"/>
                        </div>

                        <div class="product-name col-lg-10">
                            <div class="name">
                                <a href="#">
                                    <a href="{{ path('product', {'id': product.id}) }}">{{ product.name }}</a>
                                </a>

                                <div>
                                    <i class="fas fa-star full-star"></i>
                                    <i class="fas fa-star full-star"></i>
                                    <i class="fas fa-star full-star"></i>
                                    <i class="fas fa-star full-star"></i>
                                    <i class="far fa-star empty-star"></i>
                                </div>

                                <span>4/5 (1 avis)</span>
                            </div>

                            <div class="product-price">
                                <span class="discount">Prix réduit </span>
                                <span class="full-price">{{ product.productPrice }} EUR</span>

                                <br>

                                <div class="infos">
                                    <span class="delivery">Livraison gratuite possible (voir fiche produit).</span>
                                    <span class="stock">Stock : {{ product.productStock }}</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="spacer"></div>
            {% endfor %}
        </div>
    </div>
{% endblock %}